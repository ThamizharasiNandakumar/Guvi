#include <stdio.h>
int main(void) {
	char ch;
  	scanf("%c",&ch);
  	if((ch>='a'&&ch<='z')||(ch>='A'&&ch<='Z'))
      printf("character is alphabet");
	else 
      printf("character is not a alphabet");	
  	return 0;
}
